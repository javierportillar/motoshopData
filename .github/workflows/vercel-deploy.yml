name: Vercel Production Deploy

# Auto-deploy PWA a Vercel cuando hay push a main.
# Reemplaza el auto-deploy nativo (que dejó de funcionar al
# desinstalarse la Vercel GitHub App).

on:
  push:
    branches: [main]
    paths:
      - "motoshop-app/web/**"
      - ".github/workflows/vercel-deploy.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Link project and deploy
        run: |
          vercel link --project=motoshop-web \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --scope=javierportillars-projects \
            --cwd=motoshop-app/web \
            --yes 2>&1
          vercel deploy --prod \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --scope=javierportillars-projects \
            --cwd=motoshop-app/web \
            --yes 2>&1
