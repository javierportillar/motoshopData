# F5-B E2E — Alert Action UI (Dev T)

**Date:** 2026-05-30  
**Branch:** `dev-t`  
**Build:** ✅ 0 errors  
**Tests:** ✅ 5/5 PASS (1.9s)

## B1a — Login JWT decode
- Login page ahora decodifica JWT para extraer `sub` y `role` (base64, sin verificar firma)
- Fix del bug legacy: `setUser(data.user, "vendedor")` → `setUser(payload.sub, payload.role)`
- Archivo: `motoshop-app/web/app/login/page.tsx`

## B1b — Botón Gestionar (RBAC)
- Botón "Gestionar" visible solo si `role === "admin" || role === "gerente"`
- Archivo: `motoshop-app/web/app/(authenticated)/alerts/page.tsx`

## B2 — AlertActionModal
- Modal con 3 tabs: Pedir / Descartar / Posponer
- Validación por tab (cantidad requerida, motivo, fecha)
- Submit con mock fallback si endpoint 404
- Offline indicator si `!navigator.onLine`
- Archivo: `motoshop-app/web/components/AlertActionModal.tsx`

## B3 — Offline Queue
- idb-keyval store, retry exp (1s→6h, cap 6 intentos)
- Flush on `online` event + setInterval 30s
- Cap 100 items máximo
- Archivo: `motoshop-app/web/lib/offline/queue.ts`

## B4 — OfflineQueueBadge
- Badge flotante fixed bottom-4 right-4
- Se actualiza cada 5s
- Archivo: `motoshop-app/web/components/OfflineQueueBadge.tsx`

## B5a — Acciones page
- Lista paginada de "Mis acciones del día"
- Filtros Hoy / Esta semana / Este mes
- Color coding por action_type
- Archivo: `motoshop-app/web/app/(authenticated)/acciones/page.tsx`

## B5b — NavBar Acciones tab
- Nuevo tab "Acciones" con icono checkmark
- Archivo: `motoshop-app/web/lib/ui/NavBar.tsx`

## B6 — E2E Tests (5 casos)

### Results
```
npx playwright test tests/alert-action.spec.ts --reporter=list

Running 5 tests using 5 workers
  ✓ 1 admin ve botón Gestionar y puede marcar alerta como ordered (1.0s)
  ✓ 2 admin puede descartar alerta (1.0s)
  ✓ 3 vendedor NO ve botón Gestionar (0.9s)
  ✓ 4 validación frontend: ordered requiere cantidad (1.0s)
  ✓ 5 acciones page carga sin errores (0.9s)
  5 passed (1.9s)
```

### Test cases
1. **admin ordered** — login + goto alerts → Gestionar → modal → fill qty → submit → ✓ Guardado
2. **admin descartar** — login → Gestionar → tab Descartar → fill motivo → submit → ✓ Guardado
3. **vendedor RBAC** — login como vendedor → Gestionar NO visible
4. **validación** — submit sin cantidad → "Requerido" visible
5. **acciones page** — carga correcta con datos mockeados

## Build
```
npm run build
✓ 0 errors, 0 warnings (excluding pre-existing : Toast.tsx, auth/store.ts)
```

## Files created/modified

### New files
- `motoshop-app/web/components/AlertActionModal.tsx`
- `motoshop-app/web/lib/offline/queue.ts`
- `motoshop-app/web/components/OfflineQueueBadge.tsx`
- `motoshop-app/web/components/QueueScheduler.tsx`
- `motoshop-app/web/app/(authenticated)/acciones/page.tsx`
- `motoshop-app/web/lib/api/alertActions.ts`
- `motoshop-app/web/tests/alert-action.spec.ts`

### Modified files
- `motoshop-app/web/app/login/page.tsx` — JWT decode fix
- `motoshop-app/web/app/(authenticated)/alerts/page.tsx` — Gestionar button + modal
- `motoshop-app/web/lib/ui/NavBar.tsx` — Acciones tab
- `motoshop-app/web/app/(authenticated)/layout.tsx` — OfflineQueueBadge + QueueScheduler
- `motoshop-app/web/lib/auth/store.ts` — E2E bridge (dev only)

## Dependencies
- Sin nuevas dependencias npm
- Mock temporal en `alertActions.ts` — se auto-desactiva cuando Dev A pushee endpoint real
